export interface KataCompetitor {
    id: Number
    name?: String
    cal1: Number
    cal2: Number
    cal3: Number
    total?: number
}

export interface FigthCompetitor {
    color: String,
    banns: number,
    points?: number,
}